{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
"scripts": {
  "start": "node server.js",
  "frontend-build": "cd ../frontend/vite-project && npm install && npm run build",
  "backend-build": "npm install",
  "build": "npm run frontend-build && npm run backend-build",
  "dev": "nodemon server.js"
}
}
